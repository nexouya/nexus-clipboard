name: Comprehensive Multi-Distro Verification & CI

on:
  workflow_dispatch:
  push:
    branches:
      - verify-distros
      - fix-linux-clipboard-copyq-model
      - main

permissions:
  issues: write
  contents: write

jobs:
  test-ubuntu:
    name: Ubuntu 22.04 (X11 & Wayland Build & Tests)
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            build-essential \
            curl \
            wget \
            file \
            libxdo-dev \
            libxfixes-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            xvfb \
            xclip \
            wl-clipboard

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install frontend dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build frontend
        run: pnpm build

      - name: Run Cargo Check & Capture Log
        id: cargo_check
        continue-on-error: true
        run: |
          cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tee cargo_check.log
          exit ${PIPESTATUS[0]}

      - name: Post Cargo Check Log to Issue if Failed
        if: steps.cargo_check.outcome == 'failure'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const log = fs.existsSync('cargo_check.log') ? fs.readFileSync('cargo_check.log', 'utf8') : 'No log';
            const lines = log.split('\n');
            const summary = lines.slice(-100).join('\n');
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: 'Ubuntu CI Cargo Check Failure Log',
              body: '```text\n' + summary + '\n```'
            });

      - name: Enforce Cargo Check Status
        if: steps.cargo_check.outcome == 'failure'
        run: exit 1

      - name: Run Rust Tests & Capture Log
        id: rust_tests
        continue-on-error: true
        run: |
          xvfb-run -a cargo test --manifest-path src-tauri/Cargo.toml --verbose 2>&1 | tee cargo_test.log
          exit ${PIPESTATUS[0]}

      - name: Post Test Log to Issue if Failed
        if: steps.rust_tests.outcome == 'failure'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const log = fs.existsSync('cargo_test.log') ? fs.readFileSync('cargo_test.log', 'utf8') : 'No log';
            const lines = log.split('\n');
            const summary = lines.slice(-100).join('\n');
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: 'Ubuntu CI Rust Tests Failure Log',
              body: '```text\n' + summary + '\n```'
            });

      - name: Enforce Rust Tests Status
        if: steps.rust_tests.outcome == 'failure'
        run: exit 1

      - name: Run Cargo Build Release & Capture Log
        id: cargo_build
        continue-on-error: true
        run: |
          cargo build --manifest-path src-tauri/Cargo.toml --release 2>&1 | tee cargo_build.log
          exit ${PIPESTATUS[0]}

      - name: Post Cargo Build Log to Issue if Failed
        if: steps.cargo_build.outcome == 'failure'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const log = fs.existsSync('cargo_build.log') ? fs.readFileSync('cargo_build.log', 'utf8') : 'No log';
            const lines = log.split('\n');
            const summary = lines.slice(-100).join('\n');
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: 'Ubuntu CI Cargo Release Build Failure Log',
              body: '```text\n' + summary + '\n```'
            });

      - name: Enforce Cargo Build Status
        if: steps.cargo_build.outcome == 'failure'
        run: exit 1

      - name: Build Bundles (deb, rpm, appimage)
        run: pnpm tauri build --bundles deb,rpm,appimage
